(function(QUnit) {

  QUnit.module('Backbone.noConflict');

  QUnit.test('noConflict ', function(assert) {
    assert.expect(1);
    assert.equal(true, true, 'Backbone.noConflict() not implemented.');
  });

})(QUnit);
